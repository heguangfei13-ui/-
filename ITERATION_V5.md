# 本轮修正与实际数据边界

## 与上一版差异

| 用户问题 | 本轮实现 |
| --- | --- |
| 单页混入个人配置和底层健康 | `/settings` 单独承载预算、面积、用途、就业篮子、来源健康和刷新状态；主页面删除画像、现金流、健康大面板 |
| 贷款与设置脱节 | `/cashflow` 使用同一份 D1 访客设置，执行利率=LPR+加点；现金、贷款、预算上限和预留共同决定房屋预算 |
| 全指标齐全才出分 | 沿用并实际接通 available-weight 归一化，每个小区卡片直接显示独立资产分和置信度；价格/户型缺失不阻断资产分 |
| 层级只是几个标签 | 宏观、城市、板块、小区显示四个不同内容面；面包屑逐级返回，城市变更清空下钻状态 |
| 历史只有1个月 | 新增官方目录发现、2026年1—7月原表回填、原始HTML归档；1月两列价格表单独处理 |
| 只有项目名字 | 南京三个项目详情和预售PDF采集，更新具体地址、入网状态、容积率、绿地率；更正玖隐丹青府为临平区 |

## 数据规则

- 评分是透明偏好映射，不是官方评级。一个密度指标不能证明完整资产优质，所以分数保留、置信度按全部目标指标覆盖下降。
- 现售方案可能只销售车位。新房入网总数、累计成交不作为住宅总户数，不用于二手12个月成交率。
- 南京PDF为政府平台公示的开发企业申报，标记 official-reprint，设计参数覆盖置信度折减；不将拟竣工时间当实际楼龄。
- 玖隐丹青府公告的3层住宅通过透明售房网公开索引核对；原站405，保留原刊载日期、低置信度，不伪称本次源站采集成功。
- 杭州另外两个项目仍缺少可计分的权威资产输入，不能凭城市时机、普通高德路线或零库存编造小区分。
- 未采到的成交、库存、议价和自然环境证据不使用门户广告替代。杭州贝壳、我爱我家、中指口径不同的报道没有拼接为官方序列。

## 设置保存与刷新

匿名设置以128位随机Cookie标识，D1持久化；HttpOnly / SameSite=Strict / HTTPS Secure，POST校验同源。公开URL无个人配置参数。
保存后客户端立即重查已存快照并重算筛选、资产权重、现金流；同时按城市合并D1刷新请求。GitHub任务每天完整采集，每小时仅检查有无请求，无请求不抓外站。有请求采集后先入库，再回执 completed / partial。新请求不能被旧任务确认覆盖。
不是每次页面访问调用外站，也不是保存设置后假装即时拿到了所有源站数据。定时执行可能因GitHub排队延后。

## 验证

- 模型/偏好测试：缺项出分、真实部分资产分、预算硬筛、权重、现金流联动、树状父子关系。
- HTTP集成：持久化读回、不同访客隔离、CSRF拒绝、输入校验、刷新入队、采集端点鉴权。
- Python fixtures：真实NBS原表、NJ项目HTML、PDF参数口径、错误页面。
- 新建D1迁移，执行本地构建。公开站更新需单独发布确认。
