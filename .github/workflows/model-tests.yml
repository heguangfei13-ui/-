name: Decision model tests
on:
  push:
    paths: ['lib/**', 'tests/**', 'scripts/**', 'app/**', 'package*.json', '.github/workflows/model-tests.yml']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: npm ci
      - run: npm test
      - run: npx tsc --noEmit
      - run: python -m unittest tests/test_collect.py
